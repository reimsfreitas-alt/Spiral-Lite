export const runtime='edge';

type Input={payload?:unknown;channel?:unknown;opportunity_id?:unknown;tenant_id?:unknown};
const channels=new Set(['whatsapp','email','webhook']);
async function digest(value:string){const bytes=new TextEncoder().encode(value);const hash=await crypto.subtle.digest('SHA-256',bytes);return [...new Uint8Array(hash)].map(b=>b.toString(16).padStart(2,'0')).join('')}
export async function POST(request:Request){const started=performance.now();try{const body=(await request.json()) as Input;const payload=typeof body.payload==='string'?body.payload.trim():'';const channel=typeof body.channel==='string'?body.channel:'';const tenant=typeof body.tenant_id==='string'&&body.tenant_id?body.tenant_id:'demo';if(!payload)return Response.json({success:false,error:'Payload vazio.'},{status:400});if(!channels.has(channel))return Response.json({success:false,error:'Canal não permitido.'},{status:400});const opportunityId=typeof body.opportunity_id==='string'&&body.opportunity_id?body.opportunity_id:await digest(`${tenant}|${channel}|${payload}`);const idempotencyKey=await digest(`${tenant}|${opportunityId}|${channel}`);const policyVersion='lite-policy-v1';const authorization=await digest(`${tenant}|${opportunityId}|${channel}|${policyVersion}|${idempotencyKey}`);const eventHash=await digest(`NORMALIZED|${opportunityId}|${idempotencyKey}|${authorization}`);return Response.json({success:true,mode:'LOG_ONLY',opportunity_id:opportunityId,decision:'AUTHORIZED',policy_version:policyVersion,authorization:{status:'ISSUED',binding:authorization,expires_at:new Date(Date.now()+10*60*1000).toISOString()},ledger_event:{type:'AUTHORIZATION_ISSUED',hash:eventHash},idempotency_key:idempotencyKey,channel,latency_ms:Number((performance.now()-started).toFixed(2))})}catch{return Response.json({success:false,error:'Payload inválido.'},{status:400})}}
